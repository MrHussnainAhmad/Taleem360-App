import { InfoPageScreen } from '@/components/InfoPageScreen';
import { infoPages } from '@/constants/info-pages';

export default function PrivacyPolicyScreen() {
  return <InfoPageScreen page={infoPages.privacyPolicy} />;
}
